flask==3.1.3
flask-socketio==5.6.1
# Needed for Flask-SocketIO to serve real WebSockets on the Werkzeug server.
# Without it the client silently falls back to HTTP long-polling: a fresh
# thread-per-request every ~25s per client, which is the highest-churn way to
# run this and the least suited to staying up for weeks.
simple-websocket==1.1.0
# Unused: app.py pins async_mode='threading', so eventlet is never imported.
# Left installed so switching async modes doesn't need a reinstall.
eventlet==0.40.4
pyscard==2.0.7
